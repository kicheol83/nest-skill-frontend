import { Stack } from "phosphor-react";
import withLayoutOther from "../layout/LayoutOther";

const HeaderOther = () => {
  return (
    <>
      <Stack className="header-other">HEADER OTHER</Stack>
    </>
  );
};

export default withLayoutOther(HeaderOther);
