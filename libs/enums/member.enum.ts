export enum MemberType {
  USER = "USER",
  PROVIDER = "PROVIDER",
  ADMIN = "ADMIN",
}

export enum MemberStatus {
  ACTIVE = "ACTIVE",
  BLOCK = "BLOCK",
  DELETE = "DELETE",
}

export enum MemberAuthType {
  PHONE = "PHONE",
  EMAIL = "EMAIL",
  GOOGLE = "GOOGLE",
}
